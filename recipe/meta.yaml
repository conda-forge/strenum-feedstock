{% set name = "StrEnum" %}
{% set version = "0.4.10" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 898cc0ebb5054ee07400341ac1d75fdfee489d76d6df3fbc1c2eaf95971e3916

build:
  number: 0
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps

requirements:
  host:
    - python >=3.6
    - pytest-runner
    - pip
    - versioneer
  run:
    - python >=3.6

test:
  requires:
    - pip
  commands:
    - pip check
  imports:
    - strenum

about:
  home: https://github.com/irgeek/StrEnum
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: A Python Enum that inherits from str.
  description: |
    A Python Enum that inherits from str.

extra:
  recipe-maintainers:
    - timkpaine
